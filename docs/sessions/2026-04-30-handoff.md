# Session handoff: 2026-04-30

## Summary

Six audit items shipped clean. AllenComm award attribution fix, SC Broadband title-and-scope framing fix, Vitality Medical and SEO Design Chicago added to the work history, sitemap.xml redirect via Cloudflare Page Rule, client roster-size tell removed from Web Management, and multi-H1/H2 variant pattern documented with aria-hidden on alternates. All six live and verified. Parallel chat (LP#2) shipped Rolling Writer pieces in commit `774913d` during the same window; `src/content/articles/ghost-work.md` is in progress in that chat and intentionally left untracked here.

## What shipped, by commit

- `bbbd715` Fix AllenComm SPTS award attribution: count and category names. Count updated from two to four across `about.astro` hero prose (default and stars variants) and `work.astro` card body. Paraphrased "Best Advocacy Training Program" replaced with the AllenComm-listed verbatim category names: Bronze Horizon (Non-profit), Silver Horizon (Training and eLearning), Bronze Horizon (Non-profit and Advocacy), Silver Davey (General-Education). Added "View on AllenComm portfolio" verification link on the about Awards card. `work.astro` chips updated to "Horizon Awards (3)" and "Davey Award".

- `3ea415d` Fix SC Broadband title-and-scope framing: drop "sole" claim and apology. Reframed body prose across `work.astro`, `about.astro`, and `index.astro` using "all sit on me" / "running" framing. Names the work and ownership without claiming exclusivity or the director title. "Strategist" cut to "strategy" in the lists. Apology line ("The title stopped being accurate about three months in.") removed. Marketing Representative byline preserved.

- `1702f8c` Add Vitality Medical and SEO Design Chicago to work history. Two new cards in `work.astro` Web & Marketing section at Path C depth (one to two sentences, role-typical scope, no metrics): SEO Design Chicago between SC Broadband and Truvy, Vitality Medical after AllenComm. `about.astro:28` title list expanded with "copywriter" and "SEO specialist" in chronological order; closing line shifted from "went up, then down, then sideways" to "ran in parallel as often as in sequence." Default-variant narrative inserts: Vitality Medical between Iron County Today and AllenComm, SEO Design Chicago at the end of the MCC/Truvy paragraph. Stars variant, clouds variant, and `index.astro` summary skipped per Path C constraint.

- (No commit, Cloudflare Page Rule) `/sitemap.xml` 301 redirect to `/sitemap-index.xml`, added manually by Cody in the Cloudflare dashboard. Verified live: hop 1 returns 301 from the Cloudflare edge (CF-RAY present, no `x-do-app-origin`, so DO never sees the request); hop 2 returns 200 with the sitemap-index XML body. Audit-tool probes that default to `/sitemap.xml` now reach the actual sitemap.

- `6c5c44d` Remove client roster-size tell from Web Management stars CTA. Reframed "Both client sites are running on monitored uptime above the industry 99.9% threshold" to "Managed uptime tracks above the industry 99.9% threshold, monitored every 60 seconds." Subject shifted from countable inventory to uncountable property; 60-second probe added as the specificity anchor for stars-tier depth. Receipts offer ("Public data available on request") preserved. CTA pivot unchanged.

- `37d036c` Document multi-H1/H2 variant pattern and add aria-hidden on alternates. Audit flagged stacked H1 variants on `services.astro` and `contact.astro` as a semantic-HTML violation. Same pattern exists on the `index.astro` hero H2. Refactor not done (Option C); the multi-element pattern is intentional for SEO weight on segment-targeted variant copy. `swapAxis` in `Base.astro` now toggles `aria-hidden` alongside `display`. `aria-hidden="true"` added to alternates (services beach + mountain H1, contact sun + moon H1, index beach + mountain H2). Each location carries an HTML comment explaining the design decision and pointing back to the swapAxis function for the full rationale. Paragraph variants (185 across 9 files) intentionally not touched.

## Parallel chat work that landed

- `774913d` Republish three Rolling Writer pieces to `/blog/articles`. Pushed from LP#2 between this session's `3ea415d` and `1702f8c`. No conflict (different files); merged forward via fast-forward.
- `src/content/articles/ghost-work.md` is currently untracked. That's the parallel chat's in-progress work. Intentionally left alone; will commit and push from that session when ready.

## Audit context

External audit at `C:\Users\codya\OneDrive - Cody A Smith LLC\codyasmith-audit.docx` (dated 2026-04-26) drove this session. The six items shipped were audit follow-ups Cody picked from the open list in `reference_codyasmith_audit.md` plus structural items not yet entered into that reference (the Vitality Medical / SEO Design Chicago LinkedIn-vs-site gap, the multi-H1 semantic-HTML flag, the client roster-size tell, the AllenComm award count and category names, the SC Broadband "sole strategist" overreach).

## Memory updates this session

None. Relevant memories were read at session start and applied throughout; no corrections or additions surfaced that warranted memory updates.

## Deferred work

- **Phase 2 contact-flow**: qualifying-question fields on the contact form for ecosystem routing (page-count for WM segment routing, revenue/locations/team for MS). Pays off only after ecosystem routing exists.
- **Phase 3 contact-flow**: rich post-submission state with segment-aware "what happens next" preview, possibly a `/contact/sent` route.
- **Ecosystem routing buildout**: BPv2 multi-ecosystem GBB architecture documented in `business-design-v2.docx`, not yet built into the site. Upstream prerequisite for Phase 2 + 3 contact-flow and public tier ladders on service pages.
- **Search Console submission**: sitemap is reachable from robots.txt and via the new `/sitemap.xml` redirect; not yet submitted to Google.
- **Google Business Profile creation**: not yet created. Pairs naturally with Search Console for local search visibility on the Cedar City audience.
- **Status page subdomain (status.codyasmith.com)**, GA4 + Search Console + server-side logger extension, Microsoft Clarity, consent banner: Phase 3 broader site work, deferred per `project_codyasmith_portal.md`.
- **Stale apex-zone DNS records**: Mailgun CNAME and TXT, GoDaddy domainconnect, pay.codyasmith.com. Cody hasn't decided whether to delete.
- **Open audit copy items**: WordPress mismatch on Web Management page, unsourced quantitative claims, pricing disclosure inconsistency, portal not described in marketing, quiz-vs-page tone mismatch, analytics-on-vs-off paradox.
- **/listener expansion**: dedicated landing pages and SEO content per audit.

## Natural next move when work resumes

Two paths worth considering:

**Path 1: Search Console plus Google Business Profile setup.** With the sitemap redirect in place and robots.txt clean, the next move on indexing is submitting to Search Console and creating the GBP. Both are external-platform work, not repo changes. Together they kick off the indexing flywheel that the audit named as the central gap. Lower-effort than ecosystem routing, faster payoff on visibility.

**Path 2: Ecosystem routing buildout.** Highest-leverage architectural work. Unblocks Phase 2 + 3 contact-flow plus public tier ladders. Larger scope, longer timeline. Central infrastructure that makes the pricing strategy buyer-visible.

Smaller bounded alternatives if neither of the above fits: open audit copy items (WordPress mismatch, analytics paradox, portal-not-described in marketing), apex-zone DNS cleanup, `/listener` landing page expansion.

## Open threads and context for next CC

- Cody confirmed mid-session that all work is on LP#2. Single-laptop workflow; git stays linear; pulls before pushes work cleanly.
- Parallel chat is shipping blog content. Watch for additional commits from that work stream (`774913d` and the in-progress `ghost-work.md` are from there).
- Cloudflare Page Rule is now part of the routing path for `/sitemap.xml`. Not in the repo. If Cloudflare migrates or rules reset, recreate manually.
- BPv2 canonical doc at `C:\Users\codya\Downloads\business-design-v2.docx`. Read before any pricing recommendation.
- ZipKit Homes legacy contract: $500/mo ZipKit + $330/mo MVP, $830/mo total, two sites pooled. Grandfathered at signed rates; BPv2 applies to new clients only. Anniversary 2027-03-09.
- DigitalOcean App Platform behind Cloudflare hosting; deploy is push-triggered to main. This session's deploys ranged 10s to 100s.
- Astro `security.checkOrigin: true` is on. Curl POSTs to `/api/contact` without same-origin context get rejected.
- Standard memory rules in effect (voice and formatting, session checklists, no picking variants, conversation_search-first, controllers-are-unreliable, no overclaim sun, product-line discipline, no dangling tier references, personalization voice discipline, variant scope rules). All loaded automatically on session start.
